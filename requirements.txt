numpy>=1.24
opencv-python>=4.8
pyttsx3>=2.90
vosk>=0.3.45
sounddevice>=0.4.6
piper-tts>=1.2.0 ; python_version < "3.13"
flask>=3.0
# picamera2     -> install via apt: python3-picamera2 (see README §2.3)
# hailo_platform -> install from the HailoRT cp313 wheel (see README §2.5)
# vosk model    -> download into models/ (see README §2.8)
# piper voice   -> download into models/piper/ (see README §2.9)
# Note: piper-tts (piper1-gpl) only ships wheels for cp38..cp312 today.
#       On Python 3.13 the requirement above is silently skipped; the
#       app then uses the CLI piper binary (also in §2.9) instead.
